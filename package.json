{
  "name": "babel_http_and_watcher",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib",
	"server": "http-server -p 3000 ./lib",
    "watch": "npm-watch"
  },
  "author": "",
  "license": "ISC",
  "watch": {
    "build": {
      "patterns": [
        "src"
      ],
      "extensions": "js"
    }
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0",
    "http-server": "^0.9.0",
    "npm-watch": "^0.1.5"
  }
}
